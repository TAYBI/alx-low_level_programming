#include <stdio.h>
/**
 * Description: main - print alphabet
 * Return: 0 if success
 */
int main(void)
{
	int number;

	for (number = 0; number <= 9; number++)
		printf("%d", number);
	putchar('\n');

	return (0);
}
