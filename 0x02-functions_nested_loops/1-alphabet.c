#include "main.h"
/**
 * Description: print_alphabet - print lower case alphabet
 * return always 0
 */
void print_alphabet(void)
{
	char c;

	for (c = 'a'; c <= 'z'; c++)
		_putchar(c);

	return (0);
}
